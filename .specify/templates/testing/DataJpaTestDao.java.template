package org.openelisglobal.{module}.dao;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertNotNull;

import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.junit.Test;
import org.openelisglobal.BaseWebContextSensitiveTest;
import org.openelisglobal.{module}.valueholder.{Entity};
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.transaction.annotation.Transactional;

/**
 * Integration test: real Hibernate mappings and PostgreSQL queries.
 *
 * This template retains its historical filename for existing documentation links;
 * OpenELIS uses traditional Spring configuration, not Spring Boot test slices.
 * Populate the entity's actual required fields and foreign keys with owned data.
 * Prefer real services when the behavior under test is a business transition.
 *
 * Writes roll back with this test. For commit-time behavior or concurrent workers,
 * use explicit committed setup, fresh transaction reads, and teardown of exactly
 * the created records. Never wrap those cases in an outer test transaction.
 * See .specify/guides/testing-roadmap.md for ownership and test-level rules.
 */
@Transactional
public class {Entity}DAOIntegrationTest extends BaseWebContextSensitiveTest {
    @Autowired
    private {Entity}DAO {entity}DAO;

    @PersistenceContext
    private EntityManager entityManager;

    @Test
    public void insertCanBeReloadedFromTheDatabase() {
        {Entity} entity = new {Entity}();
        entity.setName("Owned persistence fixture");
        // Set all other required values and owned relationships here.
        String id = {entity}DAO.insert(entity);
        entityManager.flush();
        entityManager.clear();

        {Entity} reloaded = {entity}DAO.get(id).orElse(null);
        assertNotNull(reloaded);
        assertEquals("Owned persistence fixture", reloaded.getName());
    }

    @Test
    public void getDoesNotReturnAnUnrelatedRecordForAMissingId() {
        assertFalse({entity}DAO.get("999999999").isPresent());
    }

    // Add the actual feature query: create both matching and nonmatching owned
    // rows, clear the persistence context, and assert exact membership/order.
    // Do not settle for a non-null list or a maximum size that accepts no rows.
}
