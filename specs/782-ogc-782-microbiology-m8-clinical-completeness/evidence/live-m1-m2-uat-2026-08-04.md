# M1 + M2 live deployment and UAT evidence

## Stack and deployment

- M1 head: `56806f5c74ea5bece24c1dbaff4c830d769422c7`
- M2 application SHA:
  `8be8cd2011c3cca67ca9d95fc2996dabba986c4a`
- M1-to-M2 ancestry: M1 is an ancestor; comparison was `0 44`, so M2 was 44
  commits ahead with no M1 commits missing.
- Schema/app deployment: `20260804T074616Z-40566083060f` at
  `40566083060ff9c5769c2cfc06be1852ad5b44c6`
- Final frontend deployment: `20260804T075735Z-8be8cd2011c3`
- Target: `https://amr.openelis-global.org`
- Target contract: ready; frontend scope; non-schema-affecting; health passed;
  smoke passed. The backend and schema remain the healthy application checkpoint
  named above; the later commits add integration tests and the focused frontend
  history correction.
- Review-tooling SHA: `3d52ac103853e349df017828689b788880631d21`

The service-layer UAT scenario was provisioned after deployment and returned
the stable scenario key, accession, primary case, and sibling case recorded in
`../uat.md`.

## Checklist publication

Grist is the source of truth. Nine milestone-labeled stories and 20 stable
steps are published: nine required M1 steps, ten required M2 steps, and one
optional M1 TB reflection. The ninth story adds policy-neutral culture-media
and AST-card lot traceability without assigning requiredness semantics to Test
Catalog role labels.

The first combined publication used `hosts=amr`. The public JSON contained all
17 steps, but the overlay correctly filters by the actual browser host and
therefore rendered `0 of 0`. The story rows were corrected in place to
`amr.openelis-global.org` without changing any story or step key.

- Current checklist revision:
  `00242ff2e232998d6bab03844de68975a8dd6ef4117fd81eb51af997db4d5afe`
- Corrected host scope: `amr.openelis-global.org` on all nine stories
- Rendered overlay: all nine story headings, the M2 amendment instruction, and
  the culture-media lot instruction
  verified

## Registered Playwright verification

Command:

```bash
BASE_URL=https://amr.openelis-global.org \
EXPECTED_APP_SHA=8be8cd2011c3cca67ca9d95fc2996dabba986c4a \
EXPECTED_APP_SCOPE=frontend \
EXPECTED_SCHEMA_AFFECTING=false \
npx playwright test \
  playwright/tests/manual-only/core/ogc-782-live-uat.spec.ts \
  --project=core-live-uat --reporter=line
```

Result after the final frontend deployment: **2/2 passed in 5.7 seconds** on
2026-08-04 UTC against the exact target and current live checklist.

The journey verifies the deployment SHA/branch/scope/health/smoke guard, exact
checklist revision, unique ordered keys and requiredness, rendered M1/M2
headings, configured sidenav route, canonical worklist query state, linkable
case section state, Carbon accordion state, and breadcrumb return state. The
test uses roles, exact accessible names, URL assertions, and retrying locator
assertions. It contains no arbitrary waits, forced clicks, or private Carbon
class selectors.

The deployed foundational lot journey also passed 2/2 in 6.5 seconds. It proves
expired-lot blocking, FEFO guidance, Carbon radio interaction, exact culture and
AST consumption, and retained cross-stage history. Full-page culture and AST
screenshots were inspected after the run. The first visual pass exposed that
the AST surface hid the earlier culture row; `8be8cd201` fixes that defect, and
the final screenshot visibly contains both rows without clipping or overlap.

## Remaining acceptance and issue

- Human UAT is pending for all 19 required steps. Automation is pre-UAT
  evidence only.
- The authenticated browser console still reports anonymous 404 resource-load
  messages during navigation. They did not prevent API/UI assertions and the
  current logger does not identify their URLs. This is a non-blocking
  observability follow-up until the missing resources are identified.
- Policy-neutral lot traceability is deployed and automated evidence is green.
  `AMR-29` through `AMR-31` still require Piotr's human rulings.
