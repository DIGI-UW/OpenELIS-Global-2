# OGC-1054 Remote UAT Mapping

These are the stable required steps for the full MVP story. They are authored
in Grist only when the G0 release candidate is deployed. The old `AN-QC-*`
checklist remains historical and unpublished for final acceptance.

| Step key   | Route/state                                  | Reviewer action                                                          | Expected visible result                                                                                                                           | Acceptance IDs                     |
| ---------- | -------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| AN-MVP-001 | `/analyzers/types` with search/filter query  | Read the explainer/counts, then find and inspect a shipped Analyzer Type | Purpose, aggregate state, source, protocol, lifecycle, mapping/control-recognition completeness, use, and attention are understandable; reload/history preserve state | MVP-001, MVP-002                   |
| AN-MVP-002 | Separate Analyzer Types Create/Duplicate Profile flows and return to analyzer setup | Create a site type, use Duplicate Profile on a shipped type, then return and select it | Unique identity/revision and source lineage persist; source and pinned analyzers remain unchanged; deactivate/reactivate replaces delete; no developer identity field appears | MVP-003, MVP-004 |
| AN-MVP-003 | Analyzer Types mapping editor and Verify return state | Resolve one unmatched test, explicitly exclude another, then return to Verify | The same sole editor is used; every source row remains visible; complete active catalog search works; selections persist independently; no per-analyzer editor appears | MVP-005, MVP-006 |
| AN-MVP-004 | Result-value mapping section                 | Map a qualitative analyzer value                                         | Only active options belonging to the mapped Test are offered; save and reload preserve selection                                                  | MVP-007                            |
| AN-MVP-005 | Control-recognition and profile save-scope state | Inspect structured authoring and Verify for `RULES`/affirmed `NONE`; publish with Update shared and Duplicate Profile; explicitly adopt one new revision | Recognition is human-readable and distinct from operational QC; `NONE` states no controls are transported; no raw/fallback state appears; affected analyzers remain pinned/show Update available until explicit adoption, re-verification, and sync; duplicate identity/lineage are explicit | MVP-008, MVP-009 |
| AN-MVP-006 | `/analyzers` inline setup at Instrument      | Choose type, name, and lab units; navigate forward/back and reload       | List remains available; URL, breadcrumb, section, selected type, name, and lab units stay coherent                                                | MVP-010, MVP-011                   |
| AN-MVP-007 | Inline Verify with active and draft candidates | Confirm mappings/recognition, inspect signer metadata, change a binding, then change only operational QC | Sign-off records actor/time/profile revision/fingerprints/source rows; binding change stales only the draft and leaves the active candidate unchanged; operational-QC change stales neither | MVP-012 |
| AN-MVP-008 | Inline Connect state                         | Configure supported settings and run connection fixtures                 | Endpoint and success/failure/timeout are clear; direction choices follow capability and safe degradation                                          | MVP-013, MVP-014                   |
| AN-MVP-009 | Analyzer-scoped link to canonical Quality Control | Follow the link, exercise invalid then valid control-lot/Westgard data, and return | QC context, exact validation, and return path are clear; mapping/recognition confirmation and activation blockers do not change | MVP-015 |
| AN-MVP-010 | Completion summary and activation state      | Review the seeded incomplete candidate's blockers, complete its visible setup requirements, leave operational QC incomplete and the connection test failed, then activate | Applicable MVP-016 blockers clear with their fields; QC/test outcomes never appear; the synchronized exact candidate activates. Service/contract tests separately prove every predicate and every transition into `ACTIVE` | MVP-016, MVP-017 |
| AN-MVP-011 | Inline Verify live-capture state             | Request a known result and inspect reconciliation                        | Seen items verify, absent items remain not seen, and new items remain explicit without data loss                                                  | MVP-018                            |
| AN-MVP-012 | Draft site type created in Analyzer Types, then Inline Verify | Return to setup, request live traffic, and bind the populated rows        | Received rows populate visibly, remain held, and require explicit valid catalog choices; setup never creates or silently mutates the profile      | MVP-019, MVP-020                   |
| AN-MVP-013 | Visible demo control and Results/QC workflows | Emit a known patient result and recognized control result                | Both travel through Bridge; pinned profile alone classifies the control, which appears in QC and never as a patient result                        | MVP-018                            |
| AN-MVP-014 | Visible demo control, analyzer row, Alerts   | Emit an unknown test/value                                               | Item is held, not posted or lost; analyzer and Alerts visibly require attention                                                                   | MVP-019                            |
| AN-MVP-015 | Held-item resolution flow                    | Resolve with catalog choice and emit the same message again              | Resolution is safe and audited; next message maps without another unknown alert                                                                   | MVP-020                            |
| AN-MVP-016 | ASTM, HL7, and FILE type/setup plus visible demo controls | Exercise representative patient/control/nonmatch scenarios and the FILE workflow | Lab-facing outcomes match across protocols; automated contracts prove OR/`NONE`/no-fallback behavior and Bridge-only FILE watching/transport | MVP-021 |
| AN-MVP-017 | Bookmarked analyzer routes on desktop/mobile | Revisit completed configuration and primary routes                       | Carbon layout, headings, breadcrumbs, query state, actions, and responsive behavior remain coherent                                               | MVP-002, MVP-011, MVP-022, MVP-023 |

## Review Rules

- Fixtures establish preconditions separately; the reviewer performs the story
  through visible controls.
- Playwright cannot mark Grist steps or replace human acceptance.
- A required Fail blocks G0. N/A is invalid for a required step unless this
  contract is amended before deployment.
- The report must identify the exact deployed target and checklist revision.
