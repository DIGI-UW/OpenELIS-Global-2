# OGC-1054 Code QA Record

**Date:** 2026-07-28
**Code QA commit:** `30528d176bd128b4765242d130f38ca9fb85d7b8`

## Spec and Code Alignment

| Finding                                                                                                               | Classification              | Disposition                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| The profile UI offered readiness values absent from the shipped-profile schema.                                       | Code defect                 | The filter now uses `DRAFT`, `PENDING`, and `READY`; labels are localized and covered by a bookmarked-filter component test.          |
| Analyzer list serialization ignored `testUnit` and `analyzerType`.                                                    | Code defect                 | Client, service, controller, component, and real-Postgres contract coverage now agree with AC-1054-01.                                |
| List responses computed the full setup-verification snapshot per analyzer.                                            | Code defect/review feedback | List serialization omits the expensive snapshot; detail/create/update responses retain it.                                            |
| The audit changeset reused sequence 064 after `develop` assigned 064-067.                                             | Rebase drift                | The changeset and include are numbered 068.                                                                                           |
| Older feature specs described result import, persisted profile editing, or OpenELIS FILE polling as current MVP work. | Document drift              | Feature 004, 012, 014, OGC-41, and the roadmap now describe the implemented boundaries; git history retains prior text.               |
| The latest design mock includes a persisted/forkable profile library and an OpenELIS FILE watcher.                    | Intentional design conflict | Both are explicitly deferred/superseded. Shipped files remain read-only profile source; FILE runtime ownership remains on the bridge. |

No unresolved spec/code ambiguity remains in the application MVP. Exact-build
remote acceptance is recorded in the current evidence note.

## Meaningful Test Coverage

| Layer                    | Load-bearing guard                                                                                                                                          | Inversion evidence                                                                                                                                     |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Real Postgres/controller | Combined analyzer type and lab-unit filters return only the matching analyzer.                                                                              | Before the service/controller fix, the test returned 2 rows instead of 1.                                                                              |
| Service unit             | Status/search/type/unit normalization and deleted-row exclusion produce the expected analyzer set.                                                          | Reverting the filter overload restores the incorrect unfiltered result.                                                                                |
| Component                | URL-backed list/profile filters, canonical setup transitions, Carbon tables, breadcrumbs, and readiness states render from real router state and `en.json`. | Before remediation, the missing list controls/serialization and invalid readiness enum made the focused tests fail.                                    |
| Playwright               | One visible Instrument -> Verify -> Connect -> QC -> Review story, including reload/back/forward and qualitative catalog selection.                         | The guard rejects API access, forced controls, response polling, and arbitrary waits; the exact deployed story passed in non-video and video projects. |

The real-Postgres test owns the query/filter contract. Mockito tests do not
claim to prove database behavior. Component tests own rendering/navigation, and
Playwright owns only the assembled visible story.

## Simplicity Review

**Verdict:** Lean for the accepted MVP.

Completed cuts:

- one shared route module owns query ordering, setup URLs, and safe `returnTo`;
- one shared `PageHeader` replaces the analyzer/QC `PageTitle` path;
- one shared `AnalyzerConfigTable` owns repeated Carbon configuration tables;
- `/analyzers/new` redirects to the sole inline creation path;
- no profile, result-value mapping, or `QcRun` table was added;
- no OpenELIS FILE poller or duplicate pending-value queue was added.

Kept on purpose:

- separate setup routes are essential for saved-resource bookmarking and
  browser history;
- plugin config JSON remains the MVP override store to avoid speculative
  persistence;
- durable audit registration remains separate from mutable JSON because
  verification provenance is a compliance requirement.

## Cross-Repository Disposition

- **Bridge:** no companion PR. OpenELIS contract tests do not demonstrate a
  missing bridge behavior or loss of payload identity.
- **Review tooling:** independently mergeable companion work. The application
  remains usable without it; the tooling is required only to produce
  build-bound remote acceptance evidence.
- **OpenELIS work designs:** reference-only input pinned at
  `4c0e1a28a6904617f29a812c3a07b4a15e95d862`; no companion data change is
  required.

## Evidence Gate

**Verdict:** Pass.

- deployed application:
  `2c840a55b03b238a2ad00c987181504c2bef6ef6`;
- deployed review tooling:
  `b0e51c982a82ad967d1359e382633006907d780d`;
- checklist revision:
  `96a1e8b9c801bec7e4eeca93b7599168dbbfbb176a8b6ecbc37e64d6b28c1c83`;
- remote UAT: 8 pass, 0 fail, 0 stale, 0 required open;
- MP4: H.264/yuv420p, 800 x 450, 50.8 seconds;
- desktop/mobile frames inspected against `openelis-work@4c0e1a28`.

The screenshot audit first exposed contradictory ACTIVE copy and nested-route
service-worker registration. Both were fixed through failing focused tests,
then confirmed in the exact remote story. Binary evidence remains outside git;
the current evidence note retains textual provenance.

Two independently mergeable review-tooling defects remain: the umbrella seed
configuration points at the stale `/opt/openelis-analyzers` checkout, and the
Download action emits Markdown with embedded structured JSON instead of the
documented Markdown/JSON file pair. Neither changes application behavior or the
accepted report contents.
