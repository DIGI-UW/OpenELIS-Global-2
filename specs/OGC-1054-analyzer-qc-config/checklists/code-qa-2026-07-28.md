# OGC-1054 Historical Foundation Code QA Record

**Original date:** 2026-07-28
**Reclassified:** 2026-08-13
**Code QA commit:** `30528d176bd128b4765242d130f38ca9fb85d7b8`

This record reports the July QA pass for PR #3792's branch-defined foundation.
It is not a current code-qa run and does not establish full OGC-1054 MVP
alignment. The current acceptance contract is in
[the authoritative roadmap](../../roadmaps/ogc-1054-analyzer-feature-roadmap.md).

## Source-Boundary Correction

The original record described product-mock concepts as an “intentional design
conflict” and used them to justify a persistence decision. That reasoning was
invalid.

`openelis-work` contributes functional and visual intent only. It cannot be
used for or against an entity, table, JSON, route, API, runtime owner, or
implementation choice. Engineering decisions must be independently grounded in
current OpenELIS, Bridge, and analyzer-mock code plus engineering ADRs and
contracts.

## Historical Findings

| Finding | July disposition | Current interpretation |
| --- | --- | --- |
| Profile UI offered readiness values absent from shipped bootstrap files. | Align branch filter values and tests. | Valid branch defect/fix. |
| Analyzer list serialization ignored `testUnit` and `analyzerType`. | Fix client/service/controller/query behavior with a real Postgres test. | Valid branch defect/fix. |
| List responses computed expensive setup snapshots per row. | Remove snapshot from list serialization. | Valid performance fix. |
| Audit changeset collided after rebase. | Renumber changeset. | Valid rebase fix, subject to current rebase. |
| Older repo specs disagreed about FILE runtime ownership. | Align to Bridge-owned FILE runtime. | Valid engineering correction, grounded in `AGENTS.md` and Bridge code. |
| Product mocks showed reusable/forkable behavior not implemented by the branch. | Incorrectly called deferred technical conflict. | Functional gap only; it says nothing about persistence or repository implementation. |

## Historical Coverage

The July run meaningfully covered branch-owned behavior:

- real-Postgres analyzer list filtering;
- service normalization and deleted-row exclusion;
- component URL state, Carbon composition, breadcrumbs, setup transitions, and
  readiness presentation;
- one UI-only Instrument -> Verify -> Connect -> QC -> Review foundation story;
- guardrails against API-focused Playwright shortcuts.

It did not cover:

- Bridge-owned profile lifecycle/versioning;
- cross-repository registration/FHIR/raw-unknown contracts;
- analyzer-mock traffic through real Bridge transports;
- production unknown-result capture, hold, Alerts, and reprocessing;
- full OGC-1054 functional acceptance.

## Historical Simplicity Review

Useful branch-level reuse included shared route helpers, a shared Carbon page
header, reusable configuration tables, and one `/analyzers` creation entry.
The branch also avoided `QcRun` and an OpenELIS FILE poller.

The July conclusion that per-analyzer plugin JSON was the correct MVP profile
store is withdrawn. That was a branch implementation fact, not a validated
target architecture. The authoritative roadmap requires an engineering ADR for
the Bridge profile/OpenELIS site-binding boundary and migration away from a
create-only copied snapshot as final authority.

## Cross-Repository Correction

- **Bridge:** a companion workstream is required for profile lifecycle,
  normalized unknown context, versioned contracts, and real runtime proof. It
  is not optional merely because the July OpenELIS payload tests passed.
- **Analyzer mock:** required for deterministic ASTM, HL7, FILE, known,
  unknown, QC, connection, and two-way fixtures through Bridge.
- **Review tooling:** the merged harness remains the evidence layer; G0 requires
  a multi-repository build manifest and the full MVP checklist.
- **OpenELIS work designs:** functional/visual comparison only; no companion
  implementation or technical alignment action exists.

## Historical Evidence Result

The July foundation evidence remains valid for application SHA
`2c840a55b03b238a2ad00c987181504c2bef6ef6`, harness SHA
`b0e51c982a82ad967d1359e382633006907d780d`, and checklist revision
`96a1e8b9c801bec7e4eeca93b7599168dbbfbb176a8b6ecbc37e64d6b28c1c83`.
It is stale for current PR HEAD and insufficient for MVP acceptance.

Run `digi-uw/code-qa` again at each canonical checkpoint and at G0. The final
evidence gate must include spec/code alignment, meaningful test inversion,
simplicity/legacy removal, cross-repository status, exact build provenance,
inspected screenshots/trace, Grist report, and MP4.
