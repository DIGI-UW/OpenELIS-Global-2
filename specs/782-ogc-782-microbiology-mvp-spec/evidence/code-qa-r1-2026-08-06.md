# R1 Code-QA Record - 2026-08-06

## Scope

- Remediation base: `08b5b3888af4ba9f1c506fc555138218e0d043a4`
  (`feat/782-ogc-782-microbiology-m10-whonet-export`)
- Checked implementation/evidence head: `3ea6d82c1`
- Authoritative product source: `DIGI-UW/openelis-work` at
  `a1f720d7b3b01db63387361495f4aa6589105003`
- QA workflow source: pinned `tools/code-qa` at
  `30528d176bd128b4765242d130f38ca9fb85d7b8`

This is the final code-QA record for the remediation diff. It is intentionally
not rerun as a generic gate after deployment or human feedback. Binary
screenshots remain external artifacts; exact deployment and human UAT are
separate evidence layers.

## Meaningful Coverage

| Guard                                            | Correct level and produced evidence                                                                                                             | Inversion result                                                                                                                                                                                       |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Reference, AST-panel, and breakpoint modal focus | Carbon component tests exercise real rendered portals, keyboard open/Escape, successful save, active-dialog focus, and exact opener restoration | Reversing only the implementation from `d39deca1e` while retaining the tests produced 3 failures, 12 passes, and Carbon's unmounted-state warning. Restoring the implementation returned 15/15 passes. |
| Complete keyboard workflow                       | Registered `core-accessibility` Playwright uses the service-created scenario and real application routes/writes through amendment release       | Passes as part of the exact-head 16-entry run; it does not substitute for human accessibility review.                                                                                                  |
| Desktop/mobile WCAG signal                       | Seven desktop and seven mobile journeys scan the actual assembled surfaces                                                                      | 16/16 total including setup and keyboard journey in 1.3 minutes; the known shared shell 404 remains visible in console output.                                                                         |
| Database, ORM, and clinical writes               | Existing focused JUnit/PostgreSQL evidence is mapped per slice in the authoritative-alignment record                                            | Not rerun by this frontend-only checkpoint. T265 and final complete-diff validation remain open.                                                                                                       |

The component tests are the load-bearing guard for the modal lifecycle and focus
regression. Playwright proves assembly and user reachability. Neither test layer
claims to validate the other's failure mode.

## Spec-Code Alignment

| Area          | Current code truth                                                                                                                                                                    | Artifact status                                                                                                                                                                                                                                                                 |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-03          | Supported modern order path derives Program, persists culture details, and uses deployment Patient Origin reference data; Macro Library is external                                   | Aligned; visual, deployed, Grist, and direct Java persistence evidence remain explicit gates.                                                                                                                                                                                   |
| M-04          | Case workflow, routed actions, NCE/lost behavior, history, and focus interactions are implemented                                                                                     | Aligned; sibling-loss cascade still requires the recorded ruling, while deployed/human acceptance remains open.                                                                                                                                                                 |
| M-05          | Manual/analyzer AST, provenance, override/revert, repeat scope, report readiness, and direct keyboard behavior are implemented                                                        | Aligned; exact deployed and human acceptance remain open.                                                                                                                                                                                                                       |
| M-07          | Culture and AST grains share one worklist with canonical URL state and source-aligned operational context                                                                             | Aligned except the explicit reviewed-run queue ruling and final source-scale/deployed evidence.                                                                                                                                                                                 |
| M-12          | Microbiology consumes shared Test Catalog/Inventory reagent behavior instead of duplicating ownership                                                                                 | Aligned; required/optional/substitute policy remains a shared catalog dependency.                                                                                                                                                                                               |
| M-NFR         | Automated interaction coverage passes; worklist/case timing is measured with service-created stress data; user-facing bench APIs use existing role bundles and session-derived actors | Numeric M-NFR timings are diagnostic engineering baselines because their runtime and hardware are undefined and their 30-event case conflicts with immutable audit generation. Least-privilege Bridge ingestion auth, offline continuity, and the shared shell 404 remain open. |
| Macro Library | No macro administration/runtime is included in this remediation                                                                                                                       | Correctly separated into T274/T285 and its own future instance/UAT stack.                                                                                                                                                                                                       |

The duplicated full-remediation code-QA tasks T227/T269 were artifact drift. T227
was removed; T269 is the single final gate.

## Simplicity Review

**Verdict for the latest interaction slice: lean.** The focus-return helper has
three concrete consumers and only remembers/restores a connected element. Stable
Carbon modal shells plus keyed forms replace state-reset effects; there is no
parallel modal framework, global state store, timeout, or test-only branch.

Complexity retained on purpose:

- Canonical URL editor state is required for linkability and browser history.
- Service-created fixtures are required to exercise real clinical and reference
  behavior without SQL, fixed primary keys, or DAO bypass.
- The full remediation diff is large because the user explicitly selected one
  authoritative-alignment PR across M-03/M-04/M-05/M-07/M-12/NFR. Its review
  cost remains a delivery risk, but splitting the already selected scope into
  unrelated patches would break the requested single remediation stack.

## Commands And Results

```text
npm test -- --run src/components/admin/microbiologyReference/MicrobiologyReferenceAdmin.test.jsx
15 passed

npx eslint <seven touched reference files>
0 errors, 0 warnings

npm run pw:test:microbiology-accessibility -- --workers=1
16 passed (1.3m)
```

## Visual Evidence Closure

The registered accessibility journey now emits portable viewport captures after
locator-based readiness checks. Desktop uses the pinned mock's 1440 by 1000
viewport and mobile uses the registered Pixel 5 project. The worklist query is
scoped to the exact service-created accession and waits for its exact row, so
accumulated local fixture volume cannot produce an empty or loading screenshot.
The literal source comparison and implementation-leakage findings are recorded
in `mock-comparison-r1-2026-08-06.md`.

## Open Delivery And Architecture Gates

- T290 least-privilege Bridge analyzer-ingestion authentication is unresolved;
  generic authenticated access remains until the deployed service identity is
  known.
- Offline/intermittent behavior is explicitly not implemented; a shared
  application design is required before future work.
- T268/T272/T273 Grist publication, exact deployment, and human UAT are pending.
- The shared URL-less shell 404 remains a separately visible defect.

## Performance Requirement Health Ruling

The `<1s` Case Detail threshold is present in the original May 27, 2026 M-NFR
specification commit, but not in the workflow mock. It prescribes an engineering
fixture and timing without defining the build/runtime mode, network, measurement
boundary, or reproducible hardware. It is therefore implementation leakage in a
product artifact, not a deterministic workflow acceptance criterion. The
service-created qualification remains useful regression evidence: worklists and
filters were responsive substantially above the described volume, and the dense
case measured `1,032.7 ms` with 171 immutable events rather than the source's
internally inconsistent 30. The active-section mount cleanup is retained because
it removes real hidden work; no special-runtime tuning is justified by this
32.7 ms diagnostic variance.

## User-Facing Security Audit

The remediation maps the source's conceptual analyst/validator/manager access
to OpenELIS's existing Results/Validation/Admin role bundles instead of adding
the source document's proposed permission-code system. Class-level role checks
now cover case, isolate, AST, timeline, communication, reagent, readiness, and
reference surfaces. Final release and amendment actions require Validation or
Admin. A focused Spring Security slice proves unrelated-role `403`,
results-entry denial for final release, successful validation release, and
session actor `42` despite submitted `performedBy: 999`. Service transactions
remain the write boundary; no controller transaction was introduced.
