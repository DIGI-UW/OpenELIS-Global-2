# Code QA - R2 Order and Bench Protocol

## Scope

- Compared `dcb767238` (R1) through the current R2 branch.
- Used repository-pinned `tools/code-qa` revision
  `30528d176bd128b4765242d130f38ca9fb85d7b8`.
- Applied meaningful-test-coverage, spec-code-alignment, and simplicity-review
  to M-03 order entry, the shared order surfaces it uses, M-04 protocol change,
  service-created UAT data, and the registered Playwright journey.

## Findings And Disposition

| Severity | Finding | Evidence | Disposition |
|---|---|---|---|
| High | Collection-before-admission was blocked only in React, so a crafted save could route an impossible case. | `MicroOrderRoutingServiceImpl` received both values but validated only set count and history length. | Fixed at the existing routing service boundary before case creation. |
| Medium | `data-model.md` still described Critical Notify as current and omitted Date of Admission. | New form/submission code strips the legacy field; the entity retains it only for historical compatibility. | Engineering artifact corrected; product scope remains five editable M-03 fields plus derived protocol. |
| None | No additional implementation/spec contradiction remained after the R2 artifact sync. | Stale-claim grep, pinned M-03/M-04 comparison, and R1-to-R2 diff review. | Aligned. |

## Meaningful Coverage

The load-bearing guard is
`MicroOrderRoutingServiceTest.routeAnalysesRejectsCollectionBeforeAdmission`.
With all normal routing prerequisites satisfied and the guard absent, the test
failed with `Expected collection before admission to be rejected`. With the
service guard present, the focused JUnit 4 suite passes 16/16 and verifies no
case is created. Component tests independently cover inline correction and
action disabling; Playwright covers the assembled Carbon interaction and valid
day-over-12 save/reload path.

The other layers remain appropriately scoped:

- ORM/Liquibase tests guard the one nullable admission-date schema change and
  rollback.
- Vitest/RTL uses real Carbon roles for protocol, Requester Department/Ward,
  date input, invalid state, and clear behavior.
- Playwright uses accessible interactions and condition-based assertions. The
  R2 spec contains no `waitForTimeout` or forced action.
- The property-gated UAT scenario creates its alternate Method, Test-Method
  link, and culture setup through services with generated application IDs. It
  adds no SQL, DAO bypass, fixture endpoint, or migration.

## Simplicity Review

**Verdict: lean after the service guard.** No speculative feature or duplicate
domain model was introduced.

Kept on purpose:

- `dateUtils.js` centralizes one date-only invariant used by the shared picker,
  order transport, collection validation, and microbiology context. This
  replaces the hardcoded serializer rather than adding a second date system.
- Memoized requested-test rows and callbacks prevent Carbon table state from
  receiving new identities on every render; they do not introduce new state.
- The dedicated protocol service/action is narrower than workflow
  reclassification and preserves existing case work. It reuses Method,
  Test-Method, culture setup, case activity, and final-release guards.
- The alternate UAT Method is created only for the R1 scenario that must prove
  the M-04 Set/Change action. It is property-gated and service-owned.

## Validation

- Focused JUnit 4 routing suite: 16 passed on Java 21.
- Focused frontend suite: 5 files, 19 tests passed.
- Focused authenticated `core-app` journey: setup plus journey passed in 12.2s.
- Desktop/mobile M-03 and M-04 screenshots were inspected; no feature-level
  horizontal overflow was present.
- `git diff --check`, touched Playwright ESLint, Prettier, and the prior focused
  backend/frontend checkpoints pass.

The pre-existing global mobile header overflow is outside the R2 feature
surface and remains explicitly visible as a separate issue; it is not treated
as R2 acceptance evidence or silently hidden in the application.

## M-04 Closure Addendum

The culture-progression closure keeps the same pinned code-qa revision and
applies the three checks to the deployed-review boundary added after the main
R2 pass.

### Meaningful Test Inversion

The existing aggregate deployment/review guard is
`ogc-782-live-uat.spec.ts`. It verifies that the exact AMR application and
review-tooling revisions publish the expected host-scoped UAT story inventory,
including the four-step M-04 progression story. It is not a second UAT source
and it does not replace the per-slice backend, component, and user-journey
tests.

The separate review-tooling regression `mounts while the host application is
still loading` failed before the widget bootstrap fix and passes after
`DIGI-UW/openelis-review-tooling@f3b34f7c000c`. The aggregate OpenELIS guard
then proves the corrected checklist is attached to the intended deployment.
Neither automated guard claims that Piotr supplied human Pass/Fail/N/A marks.

### Alignment And Simplicity

- The M-04 behavior ledger now separates Specified, Implemented, Automated,
  Deployed, and Human reviewed for every observable behavior.
- `tasks.md` uses the live AMR host scope: 57 required and 3 optional steps
  across 21 stories at checklist revision `c9a3c898db53`. Macro stories remain
  in the shared catalog but are host-scoped to the separate phrases instance.
- The aggregate test uses the repository's existing authentication setup and
  leaves shared auth and Playwright project configuration unchanged.
- The aggregate test uses Carbon-accessible controls, condition-based
  readiness, and the established 120-second remote-test budget. There is no
  `waitForTimeout`, forced action, duplicate UAT contract, fixture path, or new
  abstraction.

**Verdict: lean and aligned.** The roughly 890 KB uncompressed Carbon CSS and
observed cold-load delay are recorded as an M-NFR deployment-performance
finding instead of being hidden with sleeps or presented as an M-04 feature
failure.
