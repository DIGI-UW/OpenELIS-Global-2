<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

  <!--
    OGC-803 / OGC-804 — extend the alert.alert_type CHECK constraint with the two
    in-app Alerts feature triggers raised by the reference-lab reception flow:
    REFERRAL_CRITICAL_RESULT (Critical/Abnormal returned result on Accept) and
    REFERRAL_REJECTED (lab-side acknowledgment when a returned result is rejected).
  -->
  <changeSet id="OGC-803-804-referral-alert-types" author="samuelmale">
    <sql>
      ALTER TABLE clinlims.alert DROP CONSTRAINT IF EXISTS chk_alert_type;
      ALTER TABLE clinlims.alert ADD CONSTRAINT chk_alert_type
      CHECK (alert_type IN (
        'FREEZER_TEMPERATURE', 'EQUIPMENT_FAILURE', 'INVENTORY_LOW', 'SAMPLE_TRACKING', 'OTHER',
        'EQA_DEADLINE', 'SAMPLE_EXPIRATION', 'STAT_UPCOMING', 'STAT_OVERDUE', 'CRITICAL_UNACKNOWLEDGED',
        'REQUIRED_BY_DEADLINE', 'REFERRAL_CRITICAL_RESULT', 'REFERRAL_REJECTED'
      ));
    </sql>
  </changeSet>

</databaseChangeLog>
