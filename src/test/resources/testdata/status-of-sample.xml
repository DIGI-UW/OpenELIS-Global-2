<?xml version='1.0' encoding='UTF-8'?>
<dataset>
    <!-- This file previously contained status_of_sample entries, but status_of_sample
        is a reference table managed by Liquibase. Reference tables are not truncated
        by DBUnit, so test-specific status data should be inserted directly via JdbcTemplate
        in test setUp() methods if needed. Reference data (SampleEntered, SampleDisposed,
        etc.) comes from Liquibase migrations. Test-specific data (if needed) should
        be inserted programmatically in tests. -->
</dataset>
