name: 02 - Frontend

on:
  pull_request:
  push:
    branches: [develop]
  workflow_dispatch:
  workflow_call:

env:
  DOCKER_NAME: ${{ vars.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}-frontend

jobs:
  build-prod-frontend-image:
    name: Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          # Use the host Docker daemon so buildx can see images tagged by
          # `docker build` (specifically openelis-frontend-base:local that the
          # pre-build step below produces). The default `docker-container`
          # driver runs in its own namespace and can't resolve host-tagged
          # images, which breaks our Dockerfile's `FROM openelis-frontend-base:local`.
          driver: docker

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_NAME }}

      # Pre-build the shared base image (Dockerfile.base = node + deps + source).
      # Dockerfile and Dockerfile.dev both FROM openelis-frontend-base:local,
      # so it must exist before the main build runs.
      - name: Pre-build frontend base image
        run: |
          docker build \
            -f ./frontend/Dockerfile.base \
            -t openelis-frontend-base:local \
            ./frontend

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: false

  static-checks:
    name: Static
    runs-on: ubuntu-latest
    steps:
      - name: Checkout OpenELIS-Global2
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps
        working-directory: frontend

      - name: Check Format
        run: npx prettier ./ --check
        working-directory: frontend

      - name: Enforce Playwright guards
        run: npm run pw:guard
        working-directory: frontend

      - name: Run Frontend Unit Tests (Vitest)
        run: npm test
        working-directory: frontend
        env:
          CI: true

  frontend-gate:
    name: 02 Checkpoint - Frontend
    if: always()
    needs: [static-checks, build-prod-frontend-image]
    runs-on: ubuntu-latest
    steps:
      - name: Check sub-job results
        run: |
          if [[ "${{ needs.static-checks.result }}" != "success" ]] ||
             [[ "${{ needs.build-prod-frontend-image.result }}" != "success" ]]; then
            echo "One or more sub-jobs failed:"
            echo "  static-checks: ${{ needs.static-checks.result }}"
            echo "  build-prod-frontend-image: ${{ needs.build-prod-frontend-image.result }}"
            exit 1
          fi
          echo "All frontend sub-jobs passed."
