# Standalone analyzer harness (manual). PRs to develop run this via playwright-e2e.yml
# so the harness always runs with the workflow that already exists on the base branch.
name: Analyzer E2E (Harness)

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/analyzer-e2e-reusable.yml
    secrets: inherit
